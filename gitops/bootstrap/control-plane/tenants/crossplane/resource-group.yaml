apiVersion: azure.upbound.io/v1beta1
kind: ResourceGroup
metadata:
  name: crossplane-vnet-rgv4
spec:
  forProvider:
    location: Sweden Central
  providerConfigRef:
    name: azure-hotmail
    namespace: crossplane-system